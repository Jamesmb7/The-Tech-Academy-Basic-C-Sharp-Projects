using System;


class Program
{
    static void Main() //adjectives for this method not important yet
    {
        Console.WriteLine("Hello, World!"); /*This will open the console, display hello world, then close it quickly, to prevent the close we must add Console.ReadLine() as shown below.*/
        Console.ReadLine(); /*This tells the console to stay open until we physically close it*/
    }
}

